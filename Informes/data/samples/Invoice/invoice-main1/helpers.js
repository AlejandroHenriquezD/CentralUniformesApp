function nowPlus20Days() {
    var date = new Date()
    date.setDate(date.getDate() + 20);
    return date.toLocaleDateString();
}

function total(articulos) {
    var sum = 0
    
    for (const child of articulos) {
        console.log("hashahshash");
      }
    return sum
}